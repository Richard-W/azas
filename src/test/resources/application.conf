azas {
  jdbc {
    driver = "org.h2.Driver"
    url = "jdbc:h2:/tmp/azas-test-db;MODE=MYSQL"
  }

  admin {
    password = "testpass"
  }

  scheme {
    types {
      A {
        a: String,
        b: B
      },
      B {
        b: String
      }

      Participant {
        name: String,
        email: String
      }
    }

    participantType = Participant
  }
}